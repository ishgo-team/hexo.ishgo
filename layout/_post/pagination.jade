

if page.prev || page.next
	.pagination
		if page.prev
			a.prev(href!= config.root + page.prev.path, title= page.prev.title)= page.prev.title
		if page.next
			a.next(href!= config.root + page.next.path, title= page.next.title)= page.next.title