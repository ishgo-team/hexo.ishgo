

if page.categories.length
	.categories_wrap.clearfix
		.caption
			i.fa.fa-archive
			&nbsp;
		ul.categorie
			- page.categories.sort('posts').each(function(item) {
				li
					a(href=config.root + item.path, title= item.name)= item.name
			- });